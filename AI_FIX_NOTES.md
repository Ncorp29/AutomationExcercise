# AI Fix Notes

Session: seq-1770720294828-ies5sod3l
Repository: Ncorp29/AutomationExcercise

- [1] (high) src/test/java/pageObject/ProductDetailsPage.java: The field `ViewProduct` is initialized via `driver.findElement(...)` at declaration time. This causes immediate DOM interaction before the page is loaded and before the page object is fully constructed, which results in `NoSuchElementException` on slow renders and memory retention of stale elements. Use `@FindBy` or lazy getters with explicit waits after the page is ready instead of eager lookups.
- [2] (high) src/test/java/testCases/TC_001_SignUp.java: Hard-coding signup credentials (`NCtest` / `NCtest@gmail.com`) inside the test causes sensitive or environment-specific data to leak into source control and makes the test brittle. Externalize test data via configuration, fixtures, or TestNG data providers and inject it through the base class instead.
- [3] (high) src/test/java/testCases/TC_0015_SignupMultipleUsers.java: `driver.navigate().back()` executes immediately after clicking the signup button without waiting for the confirmation dialog to appear or the page to settle. This masks signup failures, leaves the browser in an undefined state, and can cascade failures into subsequent iterations. Assert the confirmation message, ensure it is visible (explicit wait), and only then navigate away if required.
- [4] (high) src/test/java/utilities/ExcelUtility.java: Multiple `FileInputStream`/`FileOutputStream` and `XSSFWorkbook` instances are stored in public fields but never closed (no try-with-resources or finally block). Running the test suite repeatedly will leak OS handles and can lock the Excel file, especially on CI machines. Wrap file streams and workbooks in try-with-resources or provide explicit close methods to release them after every read/write.
- [5] (high) src/test/java/utilities/FileUploadUtilities.java: Runtime.exec(autoItScriptPath) executes an external script using a caller-supplied path without validation. If the path originates from test data or user input, this becomes a command‐injection vector that can execute arbitrary binaries. Additionally, Thread.sleep(3000) is a brittle wait and exceptions are swallowed (printStackTrace), so failures are silently ignored. Switch to ProcessBuilder with a fixed script location, validate/sanitize the path, block until the process exits (checking exit code), and propagate/log exceptions so test failures are visible.

